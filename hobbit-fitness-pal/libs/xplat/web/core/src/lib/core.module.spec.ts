describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { HobbitFitnessPalCoreModule } from './core.module';

describe('HobbitFitnessPalCoreModule', () => {
  it('should work', () => {
    expect(new HobbitFitnessPalCoreModule(null)).toBeDefined();
  });
});
*/
